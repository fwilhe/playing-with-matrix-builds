name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        foo: [{"x":"1"}, {"x":"1"}]
    steps:
    - uses: actions/checkout@v2
    - name: Run a multi-line script
      run: |
        echo ${{ matrix.foo.x }}
